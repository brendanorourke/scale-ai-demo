
import { ApiResponse } from '../imageAnalysis/types';

// This is a placeholder for future Anthropic API integration
export const callAnthropicApi = async (
  apiKey: string,
  imageUrl: string
): Promise<ApiResponse> => {
  throw new Error('Anthropic API integration is not yet implemented');
  
  // Implementation will follow a similar pattern to OpenAI service
  // when Anthropic API becomes available for integration
};
